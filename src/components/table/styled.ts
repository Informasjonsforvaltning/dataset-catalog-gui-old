import styled from 'styled-components';

const Table = styled.table`
  width: 100%;
  background-color: #f0f6f7;
  margin-top: 40px;
  margin-bottom: 100px;
`;

const TableBody = styled.tbody``;

export default { Table, TableBody };
