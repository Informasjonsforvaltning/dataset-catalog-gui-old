export enum KeyCode {
  TAB = 9,
  ENTER = 13,
  SPACE = 32,
  ARROW_UP = 38,
  ARROW_DOWN = 40
}

export enum RegistrationStatus {
  DRAFT = 'draft',
  APPROVE = 'approve',
  PUBLISH = 'publish'
}

export enum SearchType {
  DATASET = 'DATASET_BY_QUERY'
}
