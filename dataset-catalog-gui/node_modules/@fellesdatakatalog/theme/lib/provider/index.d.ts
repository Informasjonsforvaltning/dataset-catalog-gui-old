import { FC, PropsWithChildren } from 'react';
interface Props {
    target?: HTMLElement;
    theme?: Record<string, unknown>;
    useGlobalStyles?: boolean;
}
declare const ThemeProvider: FC<PropsWithChildren<Props>>;
export default ThemeProvider;
