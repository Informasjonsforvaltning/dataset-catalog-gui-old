import { FC, ComponentPropsWithoutRef, PropsWithChildren, ReactNode, ComponentType } from 'react';
export interface Props extends ComponentPropsWithoutRef<'a'> {
    /**
     * An alternative to href
     */
    to?: string;
    /**
     * An indication whether link points to an external resource
     * @type {boolean}
     * @default false
     */
    external?: boolean;
    /**
     * Icon placed before link text
     * @type {ReactNode}
     */
    icon?: ReactNode;
    /**
     * Component to render
     * @default a
     */
    as?: keyof JSX.IntrinsicElements | ComponentType<any>;
}
export declare const Link: FC<PropsWithChildren<Props>>;
export default Link;
