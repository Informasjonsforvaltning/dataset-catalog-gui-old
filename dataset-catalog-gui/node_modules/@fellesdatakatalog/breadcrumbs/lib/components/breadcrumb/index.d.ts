import { FC, PropsWithChildren, ComponentType } from 'react';
export interface Props {
    /**
     * An indicator whether current breadcrumb is active
     * @type {boolean}
     * @default false
     */
    active?: boolean;
    /**
     * Component to render (use li as base element)
     * @default li
     */
    as?: keyof JSX.IntrinsicElements | ComponentType<any>;
}
export declare const Breadcrumb: FC<PropsWithChildren<Props>>;
export default Breadcrumb;
