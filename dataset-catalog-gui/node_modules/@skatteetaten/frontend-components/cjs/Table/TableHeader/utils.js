"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getIconNameForTheadCell = exports.getAriaLabelForTheadCell = void 0;

const getIconNameForTheadCell = (isSorted, isSortedAscending) => {
  if (isSorted) {
    return isSortedAscending ? 'ArrowDown' : 'ArrowUp';
  }

  return 'ArrowUpDown';
};

exports.getIconNameForTheadCell = getIconNameForTheadCell;

const getAriaLabelForTheadCell = (name, fieldName, isSorted, isSortedAscending, t) => {
  let ariaLabel = fieldName;

  if (typeof name === 'string') {
    ariaLabel = isSorted ? isSortedAscending ? name.concat(' ', t('table.sorted_ascending')) : name.concat(' ', t('table.sorted_descending')) : name.concat(' ', t('table.sortable'));
  }

  return ariaLabel;
};

exports.getAriaLabelForTheadCell = getAriaLabelForTheadCell;