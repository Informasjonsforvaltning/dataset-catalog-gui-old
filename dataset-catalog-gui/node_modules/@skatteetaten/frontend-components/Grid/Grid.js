function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import * as React from 'react';
import { getClassNames } from './Grid.classNames';
import { Col } from './Col';
import { Row } from './Row';

/*
 * visibleName Grid (Rutenett)
 */
export class Grid extends React.Component {
  render() {
    const {
      children,
      className = '',
      tag = 'div'
    } = this.props;
    return /*#__PURE__*/React.createElement(tag, {
      dir: 'ltr',
      className: `${getClassNames(this.props).grid} ${className}`
    }, children);
  }

}

_defineProperty(Grid, "Col", Col);

_defineProperty(Grid, "Row", Row);

_defineProperty(Grid, "SPACE_NONE", '0px');

_defineProperty(Grid, "SPACE_SMALL", '8px');

_defineProperty(Grid, "SPACE_MEDIUM", '16px');

_defineProperty(Grid, "SPACE_LARGE", '24px');

_defineProperty(Grid, "defaultProps", {
  padding: '0 8px'
});