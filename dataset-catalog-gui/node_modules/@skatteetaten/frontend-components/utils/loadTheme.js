import { registerIcons, loadTheme, createTheme } from '@fluentui/react';
import { Palette as palette, Fonts as fonts, SkeIcons, AliasIcons } from '../utils/index'; // @ts-ignore TODO

const theme = createTheme({
  palette: palette,
  fonts,
  isInverted: false
});
loadTheme(theme); // @ts-ignore TODO

registerIcons(SkeIcons); // @ts-ignore TODO

registerIcons(AliasIcons);