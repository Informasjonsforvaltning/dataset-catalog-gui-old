import * as React from 'react';
import { CardBorder, CardColor, CardProps, CardState } from './Card.types';
export declare class Card extends React.PureComponent<CardProps, CardState> {
    static Color: typeof CardColor;
    static Border: typeof CardBorder;
    static defaultProps: {
        title: undefined;
        titleTagName: string;
        subtitle: undefined;
        titlesize: string;
        expand: boolean;
        isExpanded: boolean;
        color: CardColor;
        marginbottom: string;
        margin: string;
        buttonType: string;
    };
    constructor(props: CardProps);
    componentDidUpdate(prevProps: CardProps, prevState: CardState): void;
    render(): JSX.Element;
    _toggleExpand: () => void;
}
